{
  "extends": "../tsconfig.base.json",
  "files": [ "../src/index.ts" ],
  "include": [],                                      /* Make sure no extra includes are added. */
  "compilerOptions": {
  },
}
